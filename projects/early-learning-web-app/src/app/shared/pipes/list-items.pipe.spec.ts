import { ListItemsPipe } from './list-items.pipe';

describe('ListItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ListItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
