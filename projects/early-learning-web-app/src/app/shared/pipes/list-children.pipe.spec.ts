import { ListChildrenPipe } from './list-children.pipe';

describe('ListChildrenPipe', () => {
  it('create an instance', () => {
    const pipe = new ListChildrenPipe();
    expect(pipe).toBeTruthy();
  });
});
